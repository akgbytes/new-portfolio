import { Inter, Raleway, Signika } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const raleway = Raleway({
  subsets: ["latin"],
  variable: "--font-raleway",
});

export const signika = Signika({
  subsets: ["latin"],
  variable: "--font-signika",
});
